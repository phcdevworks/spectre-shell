# Spectre Shell Execution Todo

No active work is currently open in this package. Phases 1 through 3 are
complete — see [ROADMAP.md](ROADMAP.md) for the full delivery history and
[CHANGELOG.md](CHANGELOG.md) for release-by-release detail.

New work here remains demand-driven: it opens only when a downstream
consumer (`spectre-init`, an app in `project-web`, or a new SPA) surfaces a
concrete requirement — a starter template, a framework adapter, or a router-
signal bridge pattern repeated across two or more independent apps.

When a new phase opens, add it here with the same P0/P1/P2 structure the
completed phases used in `ROADMAP.md`.

## Explicitly Out of Scope

- Do not add routing internals here.
- Do not add reactive primitives here.
- Do not add token or styling definitions here.
- Do not expand `bootstrapApp` into a general application framework.

## Requested by Downstream

None yet.
